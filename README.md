# Electron Todo App

## Description
Simple desktop todo app built with ElectronJS. Features include adding, editing, deleting, filtering, and persisting tasks locally.

## How to run locally
```bash
npm install
npm start
```
